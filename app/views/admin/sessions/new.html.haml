%h2 Sign in

= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
  %p
    = f.label :email
    %br
    = f.email_field :email

  %p
    = f.label :password
    %br
    = f.password_field :password

  = recaptcha_tags

  %p= f.submit "Sign in"
